{
  "name": "glint-js-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "analyze": "npm run analyze --workspaces",
    "scan": "npm run scan --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {}
}
