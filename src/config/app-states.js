const AppStates = Object.freeze({
  INITIAL: "initial", // Default state before any specific state is set
  ACTIVE: "active", // Application is fully operational
  IN_MAINTENANCE: "inMaintenance", // Application is in maintenance mode
});

export default AppStates;
